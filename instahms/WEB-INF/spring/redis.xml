<?xml version="1.0" encoding="UTF-8"?>

<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:p="http://www.springframework.org/schema/p"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="
        http://www.springframework.org/schema/beans     
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context
    	http://www.springframework.org/schema/context/spring-context.xsd">

	<!-- Configurations for redis. -->
	<context:property-placeholder
		location="classpath:/java/resources/redis.properties"
		ignore-unresolvable="true"
	/>

	<!-- Connection Manager and Redis Client. -->
	<bean id="jedisConnectionFactory"
		class="org.springframework.data.redis.connection.jedis.JedisConnectionFactory"
		p:use-pool="true" p:host-name="${redis.host}" p:port="${redis.port}"
		p:password="${redis.password}" />

	<!-- Serializer for values. Since we are just sending simple strings this 
		is okay but must be changed when complex objects are used -->
	<bean id="redisValueSerializer"
		class="org.springframework.data.redis.serializer.GenericToStringSerializer">
		<constructor-arg value="java.lang.Object" />
	</bean>
	<!-- Serializer for keys;  -->
	<bean id="stringRedisSerializer" class="org.springframework.data.redis.serializer.StringRedisSerializer"/>

	<!-- Redis Template for quering redis. -->
	<bean id="redisTemplate" class="org.springframework.data.redis.core.RedisTemplate"
		p:connection-factory-ref="jedisConnectionFactory"
		p:valueSerializer-ref="redisValueSerializer"
		p:keySerializer-ref= "redisValueSerializer" />

	<!-- Adpater around the Listener for Redis events. -->
	<bean id="messageListener"
		class="org.springframework.data.redis.listener.adapter.MessageListenerAdapter">
		<constructor-arg>
			<bean class="com.insta.hms.redis.RedisMessageSubscriber" />
		</constructor-arg>
	</bean>

	<!-- Publisher for sending messages to Redis Channel. -->
	<bean id="redisMessagePublisher" class="com.insta.hms.redis.RedisMessagePublisher" />

	<!-- The channel for cache invalidation notifications. -->
	<bean id="redisChannel" class="org.springframework.data.redis.listener.ChannelTopic">
		<constructor-arg value="${redis.channel}" />
	</bean>

	<bean id="redisContainer"
		class="org.springframework.data.redis.listener.RedisMessageListenerContainer">
		<property name="connectionFactory" ref="jedisConnectionFactory" />
		<property name="messageListeners">
			<map>
				<entry key-ref="messageListener">
					<ref bean="redisChannel" />
				</entry>
			</map>
		</property>
	</bean>
	
	<!-- The channel for appointment push. -->
	<bean id="redisChannelAppointmentPush" class="org.springframework.data.redis.listener.ChannelTopic">
		<constructor-arg value="${redis.channel.appointment.push}" />
	</bean>

	<bean id="redisContainerAppointmentPush"
		class="org.springframework.data.redis.listener.RedisMessageListenerContainer">
		<property name="connectionFactory" ref="jedisConnectionFactory" />
		<property name="messageListeners">
			<map>
				<entry key-ref="messageListener">
					<ref bean="redisChannelAppointmentPush" />
				</entry>
			</map>
		</property>
	</bean>
	
	<!-- The channel for erx response push. -->
	<bean id="redisChannelERxPush" class="org.springframework.data.redis.listener.ChannelTopic">
		<constructor-arg value="${redis.channel.erx.push}" />
	</bean>

	<bean id="redisContainerERxPush"
		class="org.springframework.data.redis.listener.RedisMessageListenerContainer">
		<property name="connectionFactory" ref="jedisConnectionFactory" />
		<property name="messageListeners">
			<map>
				<entry key-ref="messageListener">
					<ref bean="redisChannelERxPush" />
				</entry>
			</map>
		</property>
	</bean>
	
</beans>